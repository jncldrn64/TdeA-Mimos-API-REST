# INFORME DE ESTADO Y TRASPASO — API REST MIMOS (SPRING BOOT)
**Fecha:** 31 de mayo de 2026
**Rama Activa:** `NewDB-3FN-Testing`

## 1. RESUMEN DEL SISTEMA Y ARQUITECTURA
- **Backend Legacy:** Aplicación monolítica en Node.js (se mantiene intacta en producción).
- **Nuevo Backend:** API REST en Java 17 + Spring Boot 3.5.14, bajo la carpeta `/api_rest/`.
- **Base de Datos:** SQL Server en contenedor **Podman** sobre host Linux. IP `10.10.1.1`, puerto `1433`, base de datos `MimosDemo`.
- **Entorno de Desarrollo:** Máquina Virtual con Windows (código fuente + IDE) usando PowerShell nativo.
- **Puerto de la API:** `8081` (el 8080 está ocupado por el legacy).

---

## 2. HITOS ALCANZADOS EN ESTA SESIÓN

### Entidades `Carrito` y `CarritoItem` — completadas y validadas
Se abordó el quinto bloque del sistema. Ambas entidades están íntimamente ligadas (un carrito contiene múltiples ítems).

Se generaron e integraron los 10 archivos de la arquitectura correspondientes a las dos entidades (Entity, Repository, Service, Controller, DTO).

**Decisiones técnicas y validaciones:**
- **Relaciones:** Se usó `@ManyToOne(fetch = FetchType.LAZY)` tanto hacia `Usuario` (en `Carrito`) como hacia `Carrito` y `Producto` (en `CarritoItem`).
- **Control de Inserciones:** Para simular transacciones reales, se validó exitosamente que la base de datos aborta y lanza un `DataIntegrityViolationException` si se intenta añadir un `CarritoItem` apuntando a un producto inexistente (`SQL Error: 547`).
- **`precioSnapshot`:** Se usó `BigDecimal` para guardar el precio del producto al momento exacto de agregarlo al carrito, previniendo que alteraciones futuras en el catálogo afecten carritos activos.

### ⚠️ BUG DETECTADO EN LA SESIÓN ANTERIOR (`Producto`)
Se le notifica a la IA encargada de la entidad `Producto` que **existe un fallo de mapeo**. Las pruebas revelaron que al hacer POST, la categoría queda como `null` en la base de datos (y los endpoints de búsqueda por categoría devuelven arreglos vacíos `[]`). 
**Causa probable:** En `ProductoService` no se está extrayendo y asignando el objeto `Categoria` desde el DTO hacia la Entidad al momento de guardar. **Se debe corregir antes de pasar a producción.**

---

## 3. 🚨 PROTOCOLO ESTRICTO DE PRUEBAS EN POWERSHELL (PARA LA OTRA IA) 🚨

**IMPORTANTE PARA LA IA DE RELEVO:** El entorno del usuario utiliza PowerShell en Windows. A lo largo de múltiples sesiones se ha comprobado que PowerShell corrompe sistemáticamente los JSON al pasarlos por consola usando variables (`$body`) o barras invertidas escapadas. 

**PROHIBIDO:** No intentes enviar comandos POST/PUT usando: `-d '{"key":"value"}'` ni `-d $body`. **Fallarán siempre** lanzando un error 400 (`JSON parse error: Unexpected character ''`).

**LA ÚNICA FORMA APROBADA Y VALIDADA PARA PRUEBAS ES:**
Utilizar la escritura a un archivo temporal nativa de Windows con codificación UTF-8 sin BOM, seguido de la lectura con `@`.

Ejemplo exacto de cómo debes entregarle los comandos de prueba al usuario:
```powershell
# 1. Escribir el JSON a un archivo temporal (OBLIGATORIO)
[System.IO.File]::WriteAllText("$env:TEMP\payload.json", '{"clave": "valor", "anidado": {"id": 1}}')

# 2. Ejecutar curl.exe leyendo el archivo (OBLIGATORIO usar curl.exe, no curl)
curl.exe -X POST http://localhost:8081/api/endpoint -H "Content-Type: application/json" -d "@$env:TEMP\payload.json"
```
Cumple este estándar sin excepciones para evitar perder el tiempo del usuario depurando errores de terminal.

---

## 4. ESTRUCTURA DE ARCHIVOS ACTUAL

```
src/main/java/co/edu/tdea/api_rest/
│
├── entity/
│   ├── Rol.java, Usuario.java, Categoria.java, Producto.java      ✅
│   ├── Carrito.java, CarritoItem.java                             ✅
│
├── repository/
│   ├── RolRepository, UsuarioRepository, CategoriaRepo, ProdRepo  ✅
│   ├── CarritoRepository, CarritoItemRepository                   ✅
│
├── service/
│   ├── RolService, UsuarioService, CategoriaService, ProdService  ✅
│   ├── CarritoService, CarritoItemService                         ✅
│
├── controller/
│   ├── RolController, UsuarioController, CategoriaCtrl, ProdCtrl  ✅
│   ├── CarritoController, CarritoItemController                   ✅
│
└── dto/
    ├── (Varios DTOs previos)                                      ✅
    ├── CarritoDTO.java                                            ✅
    └── CarritoItemDTO.java                                        ✅
```

---

## 5. CHECKLIST DE PROGRESO

- [x] `application.properties` configurado
- [x] `Rol` — completo y probado
- [x] `Usuario` — completo y probado
- [x] `Categoria` — completo y probado
- [x] `Producto` — completo (Aviso de bug: arreglar mapeo de FK `categoria`)
- [x] `Carrito` / `CarritoItem` — completo y probado (Transacciones validadas)
- [ ] `Venta` / `DetalleVenta` — **SIGUIENTE** (Capa de persistencia generada en sesión activa, pendiente de confirmación de compilación)
- [ ] `Envio`
- [ ] `PQRS`

---

## 6. GUÍA DE DESPLIEGUE LOCAL (NUEVA MÁQUINA)

1. Instalar SQL Server. Ejecutar el script base para crear `MimosDemo`.
2. Crear usuario de acceso:
   ```sql
   CREATE LOGIN api_mimos_user WITH PASSWORD = 'MimosDev2026*';
   USE [MimosDemo];
   CREATE USER api_mimos_user FOR LOGIN api_mimos_user;
   ALTER ROLE db_owner ADD MEMBER api_mimos_user;
   ```
3. Ajustar `spring.datasource.url` en `application.properties`. Sin Podman/VM, cambiar `10.10.1.1` por `127.0.0.1`.
4. Desde `/api_rest/` en consola: `.\mvnw clean spring-boot:run`


## 7. Extra.
1. Escribir el JSON a un archivo temporal (OBLIGATORIO)

[System.IO.File]::WriteAllText("$env:TEMP\payload.json", '{"clave": "valor", "anidado": {"id": 1}}')
2. Ejecutar curl.exe leyendo el archivo (OBLIGATORIO usar curl.exe, no curl)

curl.exe -X POST http://localhost:8081/api/endpoint -H "Content-Type: application/json" -d "@$env:TEMP\payload.json"

### 📝 MENSAJE PARA LA IA QUE RESOLVERÁ `ENVIO` (Entidad 7)

1. **Dependencia y Mapeo:** `Envio` depende directamente de `Venta` mediante la columna `venta_id`. Asegúrate de usar `@ManyToOne(fetch = FetchType.LAZY)` y `@JoinColumn(name = "venta_id")` en el Entity.
2. **Fechas Nulables:** Las columnas `fecha_despacho`, `fecha_entrega_estimada` y `fecha_entrega_real` deben mapearse usando `java.time.LocalDateTime`. No uses `DEFAULT` en inserción porque estas fechas nacen como nulas y se actualizan después.
3. **🚨 RESTRICCIÓN CRÍTICA DE PRUEBAS (PowerShell):** El usuario opera en un entorno Windows usando PowerShell nativo. **ESTÁ ESTRICTAMENTE PROHIBIDO** pedirle que pruebe los endpoints POST/PUT inyectando JSON directo con `-d '{"key":"value"}'` o variables de texto. PowerShell romperá el JSON.
**Único método de prueba aceptado:** Entrégale siempre un script de dos líneas usando `WriteAllText` para crear un archivo temporal, así:
```powershell
[System.IO.File]::WriteAllText("$env:TEMP\envio.json", '{"venta": {"id": 1}, "direccion": "Calle Falsa 123", "ciudad": "Medellin"}')
curl.exe -X POST http://localhost:8081/api/envios -H "Content-Type: application/json" -d "@$env:TEMP\envio.json"
